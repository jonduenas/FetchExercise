# Uncomment the next line to define a global platform for your project
platform :ios, '12.0'

target 'FetchExercise' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for FetchExercise
  pod 'SDWebImage', '~> 5.0'
  pod 'Mocker', '~> 2.5.2'

  target 'FetchExerciseTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'FetchExerciseUITests' do
    # Pods for testing
  end

end
